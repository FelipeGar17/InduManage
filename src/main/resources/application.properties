spring.application.name=indumanage

# Configuración del servidor
server.port=${PORT:8080}

# MongoDB Atlas Configuration
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://sebastian_db_user:Jhoan123@mongosebastian.4s1ce4y.mongodb.net/proyectofinal}

# Si no funciona, ve a MongoDB Atlas → Connect → Drivers y copia la cadena SIN srv
# O usa MongoDB local temporalmente:
# spring.data.mongodb.host=localhost
# spring.data.mongodb.port=27017
# spring.data.mongodb.database=indumanage

# JWT Configuration
jwt.secret=${JWT_SECRET:TuClaveSecretaSuperSeguraParaJWT2024InduManageSystemProduction}
jwt.expiration=86400000

# Logging
logging.level.com.indumanage=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.data.mongodb.core.MongoTemplate=DEBUG
logging.level.org.mongodb.driver=DEBUG
